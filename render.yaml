services:
  - type: web
    name: quanta-toolchain-demo
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 toolchain_demo:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
